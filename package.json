{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"Tests TODO.\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "snowpack": "^3.0.1"
  }
}
